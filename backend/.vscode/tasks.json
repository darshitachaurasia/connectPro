{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "shell",
            "label": "Start backend server (nodemon)",
            "command": "npm run server",
            "group": "build",
            "isBackground": true,
            "problemMatcher": []
        }
    ]
}